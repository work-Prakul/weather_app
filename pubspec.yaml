name: weather_app
description: A simple weather app using Flutter.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  bloc: ^8.0.1
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3
  # geolocator: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
